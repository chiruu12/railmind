# Copy to backend/.env and fill in. Never commit .env.
GROQ_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPGRAM_API_KEY=

# on | off — off = no LLM calls, agents use rule-only templated rationale
AGENT_LLM=on
